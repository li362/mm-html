unix_commands
ls
pwd
open < file name>
find < current directory> | grep "something" -- returns the path of the 
						file you are looking
touch <file name>
mkdir
cp <name of file 1> <directory>
cd .. -- jumps to previous dir
history 10 --- return the last 10 commands
ssh <outside server> 
